<?xml version="1.0"?>
<parameters>
  <parameter
  name="Input image"
  description=
"3D volume dataset with dimension indicated by the filename (x*y*z),
with no header"
  type="file"
  default="Input.raw"
  >
  </parameter>
  <parameter
    name="Intensity threshold"
    description=
"Determine the background regions we are not interested in and set all the
voxels below it to zero.  Use higher values for high background intensities."
    type="number"
    min="2"
    max="7"
    default="2"
  >
  </parameter>
  <parameter
    name="Connected components size"
    description=
"Used to remove objects smaller than this volume size.  Use higher values for
larger noisy objects."
    type="number"
    min="10"
    max="100"
    default="100"
  >
  </parameter>
  <parameter
    name="Anisotropic Diff k"
    description=
"Anisotropic diffusion conduction coefficient (According to smoothing strength)"
    type="number"
    min="10"
    max="800"
    default="800"
  >
  </parameter>
  <parameter
    name="Anisotropic Diff t"
    description=
"Anisotropic diffusion iterations (Higher for more iterations)"
    type="number"
    min="2"
    max="10"
    default="2"
  >
  </parameter>
</parameters>
