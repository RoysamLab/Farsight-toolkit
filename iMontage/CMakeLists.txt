PROJECT(iMontage)

INCLUDE_DIRECTORIES( ${VXL_RPL_INCLUDE_DIR} )
INCLUDE_DIRECTORIES( ${VXL_MUL_INCLUDE_DIR} )

SET(iMontage_HDRS 
)

SET(iMontage_SRCS 
	main.cpp
)

ADD_EXECUTABLE(iMontage main.cpp ${iMontage_HDRS} ${iMontage_SRCS})
TARGET_LINK_LIBRARIES(iMontage fregl)
INSTALL( TARGETS iMontage RUNTIME DESTINATION ${INSTALL_BIN_DIR} )
